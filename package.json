{
  "name": "@twilson63/request-scheduler",
  "version": "1.0.2",
  "description": "This service receives a  - notification of a scheduled request task - loads it into node-cron",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Tom",
  "license": "ISC",
  "dependencies": {
    "cron": "^1.0.9",
    "request": "^2.55.0"
  },
  "devDependencies": {
    "tap": "^1.0.4"
  }
}
